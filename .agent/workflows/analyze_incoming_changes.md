---
description: Analyze incoming changes from remote.
---
1. Run `git fetch origin`.
2. Run `git log --oneline HEAD..origin/main`.
3. Analyze the following incoming changes from origin/main. Summarize the key updates, new features, or fixes that will be applied if I pull.
