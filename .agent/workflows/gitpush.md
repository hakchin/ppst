---
description: Run `git add` `git commit` `git push` for current changes.
---

1. Run `git diff --staged`.
2. Run `git diff`.
3. Analyze the following changes and write a concise, conventional commit message. If there are staging changes, focus on them. If not, look at unstaged changes.
4. git add
5. git commit
6. git push